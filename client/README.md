# Lychee Client

Minimal React + TypeScript + Tailwind frontend for the local Lychee backend.

## Dev

```bash
npm install
npm run dev
```

Make sure the backend is running at `http://127.0.0.1:8000`.
